{
  "name": "database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "migrate": "dotenv -e .env.db -- node-pg-migrate up -m ./migrations",
    "migrate:create": "node-pg-migrate create -m ./migrations --migration-file-language sql",
    "migrate:down": "node-pg-migrate down -m ./migrations"
  },
  "author": "Dima",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv-cli": "^10.0.0",
    "node-pg-migrate": "^8.0.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
